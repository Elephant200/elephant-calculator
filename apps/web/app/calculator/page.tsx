import Workspace from "../../components/calculator/Workspace";

export const metadata = {
  title: "Workspace · The Elephant Calculator",
};

export default function CalculatorPage() {
  return <Workspace />;
}
